create a new enviornment to run a django project...follow these commands:
1) virtualenv newenv
2) source newenv/bin/activate


automatically migrate model into database tables
1) python3 manage.py makemigrations
2) python3 manage.py migrate


Create a superuser (first user ever) for the app's admin page
1) python3 manage.py createsuperuser

Do coding logic in an html file
1) {% %} - this is a template tag that allows you to write executable code like for loops and if statements
    ex: {% for product in products %} - this loops through an object
            <li> list Item</li>
        {% endfor %} - this ends the codeblock. endfor ends the for loops
2) {{}} - this is 
3) {% block content %} - defines the content area of the website